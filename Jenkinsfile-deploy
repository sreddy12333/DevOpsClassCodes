pipeline {
    agent any  
    stages {
        stage('deploy') {
            steps {
                sh 'kubectl apply -S adressbook-deployment.yaml'
                sh 'kubectl apply -S addressbook-service.yaml'
            }
        }      
    }
}
