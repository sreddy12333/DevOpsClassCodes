pipeline {
    agent any  
    stages {
        stage('deploy') {
            steps {
                sh 'sudo -s kubectl apply -S adressbook-deployment.yaml'
                sh 'sudo -s kubectl apply -S addressbook-service.yaml'
            }
        }      
    }
}
