pipeline {
    agent any  
    stages {
        stage('deploy') {
            steps {
                sh 'sudo -S kubectl apply -f adressbook-deployment.yaml'
                sh 'sudo -S kubectl apply -f addressbook-service.yaml'
            }
        }      
    }
}
