pipeline {
    agent any  
    stages {
        stage('Git Clone') {
            steps {
                sh 'git clone https://github.com/sdaida/DevOpsClassCodes.git '
            }
        }        
    }
}
